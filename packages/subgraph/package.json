{
  "name": "@robusta/subgraph",
  "license": "UNLICENSED",
  "version": "1.0.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "graph-prepare": "mustache config/config.json subgraph.template.yaml > subgraph.yaml",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ andryrkt1/option",
    "create-local": "graph create --node http://localhost:8020/ robusta/option",
    "remove-local": "graph remove --node http://localhost:8020/ robusta/option",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 robusta/option"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.18.0",
    "@graphprotocol/graph-ts": "0.18.0"
  },
  "devDependencies": {
    "mustache": "^4.2.0"
  }
}
